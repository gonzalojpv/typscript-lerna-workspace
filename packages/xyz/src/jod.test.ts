import { expect, it } from "@jest/globals";

describe("[ The Get Job Details action]", () => {
  it("should return the job details", () => {
    expect(1).toBe(1);
  });
  it.todo("should return an error if the job does not exist");
  it.todo("should return all the jobs");
});
